# Frontend Tests

Frontend tests should live here. Recommended next steps:

- Add Jest and React Native Testing Library.
- Add component tests for `CustomButton`, `CustomInput`, `CareerCard`, and `RoadmapPhaseCard`.
- Add navigation smoke tests for authentication and main tab flows.
